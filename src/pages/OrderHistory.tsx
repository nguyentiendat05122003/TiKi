import { default as CompOrderHistory } from '~/components/OrderHistory';
export default function OrderHistory() {
    return (
        <>
            <CompOrderHistory />
        </>
    );
}
